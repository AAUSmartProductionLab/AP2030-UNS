"""AAS builder v2 — IDTA-aligned semanticIds, basyx-python-sdk based.

Subclasses v1 builders from generation/AAS_generation/ and overrides the
semantic-id surface. Keeps v1 untouched and makes the diff between v1 and v2
exactly "what v2 corrected."
"""
