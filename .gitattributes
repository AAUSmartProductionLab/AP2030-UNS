# Set default behavior to auto-normalize line endings
* text=auto

# Force shell scripts to always use LF (Unix-style) line endings
*.sh text eol=lf

# Force batch files to always use CRLF (Windows-style) line endings
*.bat text eol=crlf
*.cmd text eol=crlf

# Dockerfiles and common config files should use LF
Dockerfile text eol=lf
*.dockerfile text eol=lf
docker-compose*.yml text eol=lf
*.yml text eol=lf
*.yaml text eol=lf

# Python files - LF is preferred
*.py text eol=lf

# Explicitly declare binary files (won't be modified)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.whl binary
