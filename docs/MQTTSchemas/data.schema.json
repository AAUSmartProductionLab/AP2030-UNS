{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Generic data schema",
    "description": "A base class for all data messages",
    "type": "object",
    "properties": {
        "TimeStamp": {
            "type": "string",
            "format": "date-time",
            "description": "The time stamp of the message in ISO 8601 format (xs:dateTime)"
        }
    },
    "required": ["TimeStamp"]
}