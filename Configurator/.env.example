# Configurator Frontend Environment Variables
# These are exposed to the browser, so use the host machine's IP for external access

# AAS Repository URL (for fetching AAS data)
VITE_AAS_REPOSITORY_URL=http://localhost:8081

# AAS Shell Registry URL (for discovering AAS shells)
VITE_AAS_SHELL_REGISTRY_URL=http://localhost:8082

# Root AAS Submodel ID for Hierarchical Structures
VITE_ROOT_SUBMODEL_ID=https://smartproductionlab.aau.dk/submodels/instances/aauFillingLineAAS/HierarchicalStructures

# MQTT Broker Configuration (WebSocket connection for browser)
# Note: Mosquitto uses port 9001 for WebSocket, HiveMQ uses port 8000
# When accessing from outside Docker, use the host machine's IP address
VITE_MQTT_BROKER_HOST=localhost
VITE_MQTT_BROKER_PORT=9001
