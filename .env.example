# External Host Configuration
# This IP address should be set to the host machine's IP address that can be accessed from outside the Docker network
# It's used for the AAS Web UI and other services that need to be accessed externally
EXTERNAL_HOST=192.168.0.140

# MQTT Broker Configuration
# For services running inside Docker, these default to service names
# Override these only if running services outside Docker
MQTT_BROKER=hivemq-broker
MQTT_PORT=1883

# BaSyx Configuration
# For services running inside Docker, these use internal service names
# Override these only if running services outside Docker
BASYX_URL=http://aas-env:8081
BASYX_INTERNAL_URL=http://aas-env:8081
DELEGATION_SERVICE_URL=http://operation-delegation:8087

# BaSyx Service Names (for C++ and other non-Python services)
AAS_SERVER=aas-env
AAS_PORT=8081
AAS_REGISTRY=aas-registry
AAS_REGISTRY_PORT=8080

# Planar Motor Controller IP (external hardware)
PMC_IP=192.168.100.210
